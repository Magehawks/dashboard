{% extends 'base.html.twig' %}

{% block title %}DataPrivacy{% endblock %}

{% block body %}
    <h1>DataPrivacy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ data_privacy.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ data_privacy.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_data_privacy_index') }}">back to list</a>

    <a href="{{ path('app_data_privacy_edit', {'id': data_privacy.id}) }}">edit</a>

    {{ include('data_privacy/_delete_form.html.twig') }}
{% endblock %}
